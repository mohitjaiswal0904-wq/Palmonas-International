# Product seeds by collection

One file per live collection. Combined in `../products.seed.ts`.

| File | Collection slug |
|------|-----------------|
| `ode-to-nature.ts` | `ode-to-nature` |
| `nine-kt-fine-gold.ts` | `9kt-fine-gold` |
| `essential.ts` | `essential` |

Do **not** import these from app routes — use `@/data` → built `products`.
