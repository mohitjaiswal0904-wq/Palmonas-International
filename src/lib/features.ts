/**
 * Homepage / section feature flags.
 * Flip these when you want an alternate layout version live.
 */
export const features = {
  /**
   * Category Version 2 — circular "Shop by form" grid
   * (`CategoryCircles` on the homepage).
   * Set to `true` to enable; `false` to hide.
   */
  categoryVersion2: false,
} as const;
